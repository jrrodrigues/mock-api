{
  "name": "mock-api",
  "version": "1.0.0",
  "description": "Mock API do eConsignado Trabalhador (Crédito do Trabalhador / Dataprev), gerada a partir de swagger/swagger.json",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "generate": "node scripts/generate.js && node scripts/generate-httpclient.js",
    "generate:http": "node scripts/generate-httpclient.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mock",
    "api",
    "swagger",
    "openapi",
    "econsignado-trabalhador"
  ],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "express": "^5.1.0"
  }
}
